Learny | Inscripción al taller de "#{@workshop.name}"

- if @venue.nil?
	Este taller aún no tiene un lugar asignado, en cuanto se asigne se te notificará.
- else
	El taller se impartirá en #{@venue.name}, #{@venue.address}
	Horario:
	#{@workshop.reservation.fmt_duration}

Costo:
- @workshop.free? ? "Gratuito" : "$ " + @workshop.price.to_s + " MXN"

- if @venue.nil?
	Te mantendremos informado.
	No te preocupes, el equipo de Learny te avisará cuando el taller que deseas tenga un establecimiento.
- else
	Te enviaremos una encuesta de satisfacción
	Tu opinión es muy importante, al finalizar el taller te enviaremos una encuesta para que nos cuentes tu experiencia y podamos seguir mejorando.

Afectuosamente, el equipo de Learny